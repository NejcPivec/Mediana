/* Colors */
import 'package:flutter/material.dart';

const activeColor = Color(0xFF14213D);
const inactiveColor = Color(0xFFE5E5E5);
const bgColor = Color(0xFFFFFFFF);

/* Main Title */
const mainTitleText = TextStyle(
  fontSize: 40.0,
  color: activeColor,
  fontWeight: FontWeight.bold,
);

/* Description Text */
const descriptionText = TextStyle(
  fontSize: 15.0,
  color: activeColor,
);

/* Settings Icon Text */
const settingsIconText = TextStyle(
  fontSize: 15.0,
  color: activeColor,
  fontWeight: FontWeight.bold,
);
